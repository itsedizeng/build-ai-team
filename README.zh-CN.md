# Build AI Team

[English](README.md) | [简体中文](README.zh-CN.md)

> 版本：`v1.1.0`

**先判断一个 Agent 是否足够；确需协作时，只规划最小可靠的持久 AI 团队，并给出清晰所有权、角色级模型配置和来源可核查的 Skill 推荐。**

针对多种真实项目场景优化，覆盖软件、产品、设计、研究、数据、内容、系统迁移和发布等工作。

给 Build AI Team 一个真实需求。它会从最终结果倒推：一个 Agent 够用就不拆；只有职责所有权、隔离或协作价值明确时，才增加持久角色。

```text
你的需求 → 一个 Agent 或最小可靠团队
→ 角色所有权与模型配置 → 由你确认
```

## 核心能力

- **团队够用就好。** 先判断一个 Agent 是否足够；只有职责所有权、隔离或协作价值明确时，才增加持久角色。
- **窄问题只回答窄范围。** 已有团队只给 `add／merge／replace／keep` 差异；确认替换后，先创建并验证新任务，再更新受影响职责、复核状态，最后归档旧任务。模型和 Skill 单问只回答当前请求的具体配置。
- **每个角色都有明确结果。** 当前角色都要写清唯一责任、负责结果、依赖和边界。
- **按责任推荐模型。** 给出默认模型和推理强度，并说明升级条件；安全可行时再给节省 Token 的模型配置。
- **区分本地 Skill 与公开候选。** 用户要求公开搜索时，再核对适用性、来源、维护信号和权限风险，不把推荐说成已安装或安全认证。
- **继续利用当前规划对话。** 用户确认后，当前对话可在适用时继续担任有实质工作的主控，只创建其余必要的持久任务。真实持久任务就是团队状态，不生成 `AI_TEAM.md` 成员清单。
- **新项目默认独立判断。** 相同文件夹、相似标题、旧任务、记忆或旧团队方案不能单独证明是同一项目；只有用户明确继续同一项工作时，才复用既有范围与任务。
- **把重要动作分开。** 规划、建队、公开搜索 Skill、安装、登录、私有数据访问、写入、上传、发布和删除不是同一次授权。

> 规划 ≠ 建队 ≠ 搜索 Skill ≠ 安装 ≠ 外部权限。

## 快速开始

当用户针对具体项目、角色或工作包询问新团队方案、已有团队调整、模型配置或项目级 Skill 建议时，Codex 可以自动匹配 Build AI Team，并选择满足请求的最窄模式；用户不需要额外写“启用 Skill”。

简单且边界清楚的直接任务、普通讨论或提及、明确否定调用，以及 `TOOLKIT_MODE=member` 成员工作不会进入团队规划。自动匹配仍取决于宿主的 Skill 选择，因此 `$build-ai-team` 继续作为最可靠的显式备用入口。

安装后新建 Codex 任务，直接输入一个真实需求：

```text
我想做一个能上架 App Store 的记账 App。请判断需要一个 Agent 还是多个 Agent，并给我完整工作安排。
```

第一次响应只给方案。没有针对具体动作的确认，不会建队、安装 Skill、连接账号、读取私有数据或写入外部系统。

## 支持与验证范围

- 英文是唯一运行语义来源；澄清问题和完整建议跟随用户最新实质请求的语言，技术标识保持准确。
- `v1.1.0` 五份运行文件已经通过独立静态验收，结果为 0 重大／0 轻微／0 阻断。
- 冻结的四模式基线完成了 9/9 个技术有效 Run 和 10/10 条 response。独立验收定位出一处公开 Skill 用户可见状态契约歧义；未触发和 member 优先路径中的宿主表现只保留为宿主观察，不归因给没有运行的 Skill。
- 公开 Skill 输出契约只修改 `skill-discovery.md` 后，新的 2 Case／3 context／5 response 差异批次达到技术 3/3，独立内容验收为 5 通过／0 轻微／0 重大／0 阻断。
- 随后一处窄范围 `SKILL.md` 修订要求替换确认直接写出完整安全任务顺序。新的 saved-project 两轮动态测试独立验收为 1 通过／0 轻微／0 重大／0 阻断，并实际验证：创建并确认新成员、更新并确认受影响现有职责、复核两边任务状态、最后归档旧任务且不删除。
- 这是组合差异证据，不表示当前运行文件完整重跑了全部 9 Case。既有 `v1.0.1` Codex Desktop 结果继续作为未改动的团队规划、所有权、权限和建队行为证据。
- 这些结果只代表有限的真实宿主观察，不构成统计保证或跨宿主验证。
- Codex Desktop 的项目工作方式是当前核心目标环境。
- 已提供 ChatGPT Work 和 Claude 的兼容入口说明，但本版本尚未在这些宿主中完成实机验证。
- 本项目不是 OpenAI、Codex、ChatGPT 或 Anthropic 官方项目。
- 宿主版本、模型可用性和建议质量可能不同；不暗示任何厂商认可，也不保证跨宿主兼容。
- 本版本延续从公共 `v1.0.0-rc.9` commit 开始采用的 MIT License。

## 它不会自动做什么

- 创建、替换、重新分配或归档 Agent／长期任务；
- 安装或运行第三方 Skill；
- 连接账号或业务系统；
- 读取私有数据；
- 写入、上传、发布或删除内容；
- 保证第三方 Skill 安全、最新或一定适用；
- 保证所有行业只有唯一正确的团队人数。

## 在 Codex 项目中安装

1. 下载或克隆本仓库。
2. 将完整的 [`build-ai-team/`](build-ai-team/) 文件夹复制到目标项目的 `.agents/skills/` 目录。
3. 最终路径应为：

```text
<project>/.agents/skills/build-ai-team/SKILL.md
```

4. 同一版本的五份运行文件必须保持完整：

```text
build-ai-team/
├── SKILL.md
├── agents/
│   └── openai.yaml
└── references/
    ├── curated-skills.md
    ├── model-routing.md
    └── skill-discovery.md
```

5. 新建一个 Codex 任务，直接描述一个可能需要团队规划的实质项目：

```text
我想做一个能上架 App Store 的记账 App。请判断需要一个 Agent 还是多个 Agent。
```

如果宿主没有自动匹配，可使用显式备用入口：

```text
$build-ai-team 我想做一个能上架 App Store 的记账 App，请给我团队建议。
```

不要只复制 `SKILL.md`，也不要把整个仓库根目录放进 `.agents/skills/`。更新时请整体替换五份运行文件，不要混用不同版本。

如果 Skill 没有出现，先检查目录层级、`build-ai-team` 文件夹名称和五份文件是否完整，再考虑其他处理方式。

## 更新或卸载

- **更新：** 用同一个新版本的完整 `build-ai-team/` 文件夹替换原来的五份运行文件，核对 `SHA256SUMS.txt`，然后新建 Codex 任务。不要混用不同版本的文件。
- **卸载：** 只删除 `<project>/.agents/skills/build-ai-team/`，然后新建 Codex 任务。不要删除上层 `.agents/skills/` 目录，因为其中可能还有其他 Skill。

卸载后，后续新任务不再加载本 Skill；它不会撤销用户此前另行确认创建的团队、文件、安装、账号访问或外部动作。

## 示例需求

```text
我只需要根据已经定稿的 PRD 做一个可点击原型。请判断一个 Agent 是否足够，不需要开发。
```

```text
请规划一次 CRM 数据迁移，并安排独立的只读核验；现在不要连接 CRM。
```

```text
I want to design, build, and launch a paid web app with accounts and subscriptions. Decide whether one Agent or a team is appropriate.
```

```text
当前团队有一名后端负责人和一名只读核验者。迁移任务需要新增成员，还是保持现有团队？
```

```text
数据迁移核验工作应该用哪个模型和推理强度？
```

```text
当前本地有哪些 Skill 适合检查 iOS 上架准备？
```

## 如何理解团队方案

- `Required／必要`：方案确认后属于当前团队。
- `Required if…／具体条件成立时必要`：只有可观察条件成立后才出现该工作包，当前不创建。
- `Optional／建议`：可以提高质量、速度或覆盖度，但不是完成当前目标所必需，默认不创建。
- `Owner`：某个工作包或交付物的唯一负责人。
- `project lead／主控`：由一个已有实质角色兼任，额外负责计划、依赖和最终交接状态；不会自动取得其他专业交付的所有权。
- 独立验证者：不参与被审对象的生产，也不修改被审结果。

## 本地适用 Skill 与公开搜索

首次建议只使用当前宿主直接暴露的本地 Skill metadata，不显示未经现场核验的公开候选。只有用户明确要求时，才开始公开来源的实时只读搜索；搜索不会重复本地列表，若已有团队方案，也会继续保留原方案的主要下一步。

推荐不等于安装；安装也不等于允许运行脚本、登录、读取私有数据、写入、上传、发布或删除。使用第三方 Skill 前仍应核对文件、权限和许可。

第三方项目名称和链接仅作为参考，不代表认可、合作关系、当前兼容或安全认证；这些项目仍适用各自的许可。

## SHA-256 校验

[`SHA256SUMS.txt`](SHA256SUMS.txt) 只覆盖五份运行文件。下载后请使用系统支持的 SHA-256 工具进行核对。

## 仓库文档

- [English README](README.md)
- [CHANGELOG](CHANGELOG.md)
- [贡献说明](CONTRIBUTING.md)
- [安全说明](SECURITY.md)
- [MIT License](LICENSE)

## 与 Agent Team v1.0 的关系

Build AI Team 是独立的 Skill 产品，不替代早期的五文件 [Agent Team v1.0](https://github.com/itsedizeng/agent-team) 工具包及其不可变 Release。

## 开源许可与唯一官方来源

本版本按 [MIT License](LICENSE) 提供。许可变更已从包含 `v1.0.0-rc.9` 的公共 `main` commit 开始生效；该版本及后续明确采用 MIT 的版本可以在遵守许可的前提下使用、复制、修改、合并、发布、分发、转授权和销售副本。

早期公开的 `v1.0-beta7` 源码快照仍受[该次提交中附带的非开源定制许可](https://github.com/itsedizeng/build-ai-team/blob/b2e81d3859a169056629e78dff322cfcfbefcf31/LICENSE.md)约束；MIT 变更不追溯适用于旧快照。

唯一官方仓库是 [itsedizeng/build-ai-team](https://github.com/itsedizeng/build-ai-team)。
